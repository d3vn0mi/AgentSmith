/* New mission modal — stub. Task 13 implements the real form. */
import { toast } from '../toast.js';
export function openNewMissionModal() {
    toast.info('New mission modal — coming in Task 13');
}
